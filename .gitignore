*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
app/build
/captures
.externalNativeBuild
.cxx
local.properties

# Keystore — never commit these
keystore.jks
keystore.properties

# Sensitive / local-only files
claude k
tkn
*.key
*.token
.claude/

# Kotlin build artifacts (not source)
.kotlin/

# Large binaries / build outputs — never commit (releases live on GitHub)
*.apk
free-exercise-db-main.zip
Change docs/Archive/sample-set.zip
Change docs/Archive/sample-set/

# Agent scratch / debug dumps
scratchpad/
