<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Stage-3 item 3: the aggregate overview graphs are gone — Recap is purely the
             per-session view; the Stats tab keeps all aggregates. -->

        <!-- B7: browse monthly Wrapped stories (hidden until an ended month has data) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_monthly_wrapped"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:text="Monthly Wrapped"
            android:visibility="gone" />

        <!-- Session selector -->
        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/til_session"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:hint="Session"
            style="@style/Widget.Material3.TextInputLayout.OutlinedBox.ExposedDropdownMenu">

            <AutoCompleteTextView
                android:id="@+id/ac_session"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="none"
                android:focusable="false" />
        </com.google.android.material.textfield.TextInputLayout>

        <!-- QoL 2026-08 item 02: skeleton removed — content simply appears when ready. -->

        <!-- Empty / first-time state -->
        <TextView
            android:id="@+id/tv_recap_empty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="No sessions yet. Complete a workout to see your recap."
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:gravity="center"
            android:paddingVertical="24dp"
            android:drawableTop="@drawable/ill_node_network"
            android:drawablePadding="16dp"
            android:visibility="gone" />

        <!-- Programmatically-built recap sections -->
        <LinearLayout
            android:id="@+id/layout_recap"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

    </LinearLayout>

</androidx.core.widget.NestedScrollView>
