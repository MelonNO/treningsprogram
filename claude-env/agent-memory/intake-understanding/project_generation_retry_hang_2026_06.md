---
name: generation-retry-hang-2026-06
description: Generation reliability batch at docs/intake/generation-retry-hang-2026-06/ — H1 hang + H3 under-fill + H2 blacklist (shipped v1.10.3) + H4 v1.10.3 REGRESSION (large full-gen + weekly summary time out at 180s read timeout, user fully blocked)
metadata:
  type: project
---

Intake batch produced 2026-06-27 at `docs/intake/generation-retry-hang-2026-06/` (INDEX.md + brief-H1 + brief-H3 + brief-H2 + evidence/). Source: user bug report relayed via coordinator, with strong primary evidence (Prompt-Log screenshot + captured attempt-1 prompt & response, copied into `evidence/`). **Status: FINAL + crosschecked. User DIRECTLY confirmed restatement ("Yes, that's right") AND scope ("Both — never hang AND get a plan"). Finalizing docs ≠ dispatch (separate coordinator/user action).**

**The user's phrase decoded:** "verification prompt" = the second Claude call `validateProgram` (prompt opens "You are a sports science peer reviewer"; on-screen progress "Reviewing plan for quality…"; logs a "validate" Prompt-Log entry). No code string literally says "verify" — it is the user's own term. Their mental model was correct: generate ran + logged, verify never ran/logged, nothing saved.

**H1 (HIGH, diagnose-first) — the bug:** On build 1.10.2, "Generate AI Program" (Program tab) → generates attempt 1, then UI freezes on "Generating/Attempt 2 of 3" indefinitely (observed ~8+ min). Prompt Log shows ONLY `GENERATE ATTEMPT 1`; no attempt 2/3, no validate entry; NO error; nothing saved. 100% repro, every time.
- **Trigger (CONFIRMED via the captured response):** all 5 days estimate UNDER the 40-min floor (38/34/34/39/37 by `WorkoutTimeEstimator`; window 40–60) → strict per-day time-budget gate rejects attempt 1 → `validateProgram` is correctly SKIPPED on a deterministic rejection (so no verify, matches user "never saw Reviewing…") → loop advances to attempt 2. Same seam as v1.10.1/G1; prompt fix still doesn't land days in-window. This is the trigger, NOT the new defect.
- **New defect (root cause UNCONFIRMED — left for orchestrator):** loop never gets past attempt 2; attempt-2 prompt never logged (the log write is right after the attempt-2 call returns), so stall is at/just-before the attempt-2 generate call.
- **KEY NUANCE I flagged (corrects the coordinator's "no timeout" guess):** OkHttp client DOES have timeouts — connect 30s/read 180s/write 30s + **callTimeout 240s**, and each call wrapped in **withAiRetry(2)**. So a stalled attempt-2 network call would eventually error after up to ~2×240s ≈ 8 min — right at the observed window. So "infinite" may be "very long but bounded"; orchestrator must decide between (a) genuinely unbounded hang in non-network code vs (b) long network stall whose eventual error isn't reaching the user (user was on Prompt-Log/Profile screen, not Program tab, when observing). `validateProgram` is fail-open (`runCatching{…}.getOrElse{accepted=true}`) and only logs on success — consistent with "verify never ran."
- **H1 acceptance:** generation ALWAYS reaches a bounded terminal outcome — saved plan (verify runs when pre-checks pass) OR a clear, promptly-visible error (incl. when user is on a different screen than Program tab) — NEVER a frozen "Attempt N of 3".

**H3 (HIGH, diagnose-first) — the "get a plan" half (user chose BOTH, 2026-06-27):** generation must reliably PRODUCE+SAVE a plan whose days pass the strict gate (≥40, ~50). Same class as v1.10.1/G1 BUT v1.10.1 already added heavy steering and model is STILL under-filling (34–39) → orchestrator must DIAGNOSE WHY before re-patching (candidates: model mis-applies/ignores formula; under-fill self-check skipped because JSON-first; formula under-counts; **per-session ≤18–20-set cap + ≤120s hypertrophy rest STRUCTURALLY collides with 50-min target** — my arithmetic: 6-ex day naturally lands ~37–42 min within caps). **FLAGGED ESCALATION:** if caps vs 50-min target are structurally unsatisfiable, orchestrator must report BACK to user — NOT relax gate/caps/add salvage. Fix is PROMPT-SIDE ONLY; gate stays strict.
- **HARD constraint carried from G1 (applies to H1 AND H3):** keep time-budget gate strict; NO salvage/fallback/window-widening/bypass.
- **Combined acceptance:** Generate reliably produces+SAVES a gate-passing plan (verify runs on it) within bounded time; else fails fast with a clear visible error; NEVER hangs; gate never weakened.

**H2 (Medium, diagnose-first) — secondary, does NOT cause the hang:** the generation prompt's EXERCISE BLACKLIST is built in `buildPrompt` by `Regex(":\\s*(.+)")...split(",")` over `previousPlan` → exercise names containing commas (e.g. "Dumbbell Push Press (Seated, Alternating)") shatter into junk tokens ("Alternating)", "Slow Tempo)", "(Seated", …) and non-exercise colon-lines ("Mon: …") get ingested. Regex is linear (not the hang). Acceptance: blacklist lists whole names (commas intact), no junk, no non-exercise lines.

**H4 (URGENT, diagnose-first) — v1.10.3 REGRESSION (added 2026-06-27 after H1/H2/H3 shipped as v1.10.3):** full-generate + weekly coach summary now TIME OUT at the 180s read timeout ("The AI request timed out. Please check your network connection…") under a working network, logging NOTHING (Prompt Log only writes after a response returns); user fully blocked, every entry point, every time. Single-day regen WORKS (small request <180s); weekly + full-generate BROKEN (large request >180s). User-direct-confirmed. CODE-GROUNDED: msg = SocketTimeout branch of friendlyAiErrorMessage (180s read timeout, NOT the 360s H1 deadline nor 240s callTimeout); H1's `isTransientGenerationError = isTransientAiError && t !is SocketTimeoutException` makes a generate timeout TERMINAL (no retry); weekly uses DEFAULT retry (retries once on timeout then fails). Leading mechanism (CONFIRM vs LIVE API): H3 steers fuller/larger plans → response >180s → H1 no-retry makes it terminal. **Live key provided at `/home/migul/treningsprogram/claude k` (gitignored/untracked — NEVER print/commit/copy); orchestrator MUST reproduce+measure latency/size vs the live endpoint and verify the fix end-to-end before re-ship.** Acceptance: large full-gen + weekly complete+save+log under normal network (no spurious timeout) OR fail with a clear CORRECT prompt error; must NOT reintroduce the hang (H1) or weaken the gate (H3). Open Qs: was weekly broken pre-1.10.3 (its path unchanged by batch — maybe latent/environmental)? right fix (timeout raise / bounded retry / streaming / smaller output)? log timed-out attempts too?

**Cluster:** H4(urgent)+H1+H3+H2 = ONE worker (all in AiRepository; do NOT split across workers on this file). NOW: H4 FIRST (unblock timeouts), then re-verify H1 (never-hang) + H3 (gate strict, days in-window) still hold under the H4 fix, then H2 (already shipped). Earlier order (pre-H4) was H3→H1→H2.

**How to apply:** if the user revisits AI-generation reliability / the time-budget gate / generation hangs / under-fill, this batch is authoritative. Same hot seam as [[generation-timebudget-fix-2026-06]] (v1.10.1/G1) and the v1.10.0 B08/B09/B10 work. See [[intake-doc-format]]. FINAL + crosschecked; not yet dispatched to orchestrator at time of writing.
