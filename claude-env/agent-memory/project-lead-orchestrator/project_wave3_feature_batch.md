---
name: wave3-feature-batch
description: Wave 3 (E2 programs+mesocycles: storage→generation(L1)→deload(M2)) — design plan, Room 12→13 path, seams, dispatched 2026-06-24
metadata:
  type: project
---

Wave 3 of feature-batch-2026-06 (see [[wave2-feature-batch]], [[wave1-feature-batch]], `docs/intake/feature-batch-2026-06/SEQUENCE.md` + `brief-E2-programs-and-mesocycles.md`). ONE worker, internal order STRICT: E2-storage → E2-generation(L1) → E2-deload(M2). User ASLEEP, full autonomous authorization incl. REAL Claude API calls on device for L1 verify. Wave 4 (E1) builds DIRECTLY on E2's settled storage model — get it right.

**Starting state (verified at dispatch, 2026-06-24):**
- Room version **12** (Wave 2 baseline). E2 bumps → **13**, one migration MIGRATION_12_13, registered in BOTH AppDatabase companion AND DatabaseModule list (gotcha: list lives in di/DatabaseModule, missing there = runtime throw).
- AiRepository coherent post-Wave-2: `generateAdaptedProgram` (line 136) returns `Result<GenerationResult(exercises, attemptCount, rejectionReasons, rationale)>`; `ProgramJson(days, rationale)`; B3 STALLED-LIFTS block in `buildSessionHistory`; B2 `parseRationale`. `generateSingleDayProgram` (758), `generateWeeklySummary` (236) separate.
- Plan storage: `planned_exercises` keyed (weekStart, dayOfWeek, orderInDay). DAO `PlannedExerciseDao`. Repo `WorkoutRepository.savePlan(weekStart,...)` (157, delete+insert week), `saveDayPlan` (164, preserves week rationale), `getPlannedForWeek/Day`, `getLatestPlanWeekStart`, `getAllPlannedOnce`.
- Helpers in WorkoutRepository.kt: `thisMonday()` (303), `currentDayOfWeek()` (326), `autoGenWeekKey()` (324).
- Consumers of weekStart keying: HomeViewModel.init (117 getPlannedForDay(thisMonday(),currentDayOfWeek())); MainActivity.checkAndAutoGenerateWeeklyPlan (236, savePlan(monday,...)); ProgramViewModel.regenerateDay (123 saveDayPlan); ProgramFragment lists weekPlan/selectedDay.
- StallDetector public: `isStalled(history)` (73), `stalledExercises(map)` (92), `suggestionFor(name)` (102). STALL_WINDOW=3, e1RM via Epley.

**E2-storage design (CHOSEN model — minimal coherent re-key, NOT a full weekStart rewrite):**
- New `Program` entity (id, name, createdAtMs, isActive/active pointer, mesocycle fields: blockStartWeek/mesocycleWeeks or isMesocycle flag, isFrozen for assumption N, isDeload current-state). Add nullable `programId: Long?` FK column to planned_exercises.
- Active-program concept: single active program drives Home today-view + Program tab. Switching = change active pointer, queries filter by active programId.
- KEEP weekStart/dayOfWeek/orderInDay keying WITHIN each program (don't rewrite the time math the whole app + backup depends on). Plans become (programId, weekStart, dayOfWeek)-scoped.
- MIGRATION_12_13: CREATE TABLE programs + ALTER planned_exercises ADD COLUMN programId. Backfill: create a default "My Program" active program, set existing rows' programId to it (so single-program/no-block users get sensible default — acceptance criterion).
- savePlan/saveDayPlan/getPlannedForWeek/Day must become programId-aware (default to active program). HomeViewModel + MainActivity + ProgramViewModel thread active programId.

**E2-generation (L1):** make generateAdaptedProgram mesocycle/program-aware. Per L1: progression = EXISTING weekly AI adaptation on logged performance (NOT a fixed ramp) — per-week API call accepted. Edit prompt to note block/week position + deload state. KEEP B3 STALLED-LIFTS block + B2 rationale intact. Assumption N: AI weekly auto-adapt still runs within a named program unless isFrozen.

**E2-deload (M2):** stall/fatigue-TRIGGERED (not fixed final week). REUSE domain/StallDetector (do NOT reimplement). When stalls/fatigue detected → enter deload state on active program, signal into generation prompt + show user a deload is happening. Hard prereq B3 = Wave 1 PASS (done).

**Backup-inclusion decision:** Program entity is CORE user data (named saved programs) — higher impact than B1 summaries to lose on restore. BackupEnvelope (data/backup/BackupModels.kt, CURRENT_BACKUP_VERSION=2) enumerates tables EXPLICITLY; planned_exercises already in set (new programId column rides through Gson whole-entity automatically, like B2 rationale did). Adding `programs` table cleanly needs: envelope field + mergePrograms + export/import wiring + version 2→3 + BackupMigrations V2_TO_V3 + test updates. DECISION recorded in report (do or defer-with-risk per brief "don't block").

**Wave 3 gate (all 4, evidence-backed):** debug+release build green (md5); `./build.sh test` counts from result XMLs naming new E2 tests; on-device ui-test-worker for (a) program save/switch (b) mesocycle block progression (c) TRIGGERED deload (drive stall so deload fires, reusing B3); AiRepository coherent (B3/B2 intact); Room migrations coherent ordered; record final Room version (13) = E1 baseline; settled program/storage model summary for E1.

**Hard constraints:** build via ./build.sh only. NO commits/tags/releases/staging. Coherent tree. Leave flows/ alone (coordinator cleans at ship). Stay in Wave 3 (do NOT start E1). Do NOT roll into Wave 4 — coordinator gates wave transitions.

**WORKER WORKTREE GOTCHA (2026-06-24):** the worktree agent branched from `db495ec Release v1.7.0` (BEFORE Waves 1-2, which are UNCOMMITTED working-tree changes on main). Worker correctly synced Wave1/2 files into the worktree first, so E2 rebased on B2/B3 not blind. Lead had to INTEGRATE E2 back into the MAIN working tree (the real Wave-4 baseline): verified all Wave1/2-only files byte-IDENTICAL main vs worktree (no drift), then copied the 19 E2-modified + 7 E2-new files into main. Per-file diff main↔worktree = pure E2 delta (B2 rationale/B1 WeeklySummary::class/Wave2 migration list all preserved). For any future worktree worker: confirm its base commit; if stale, the lead integrates the delta into main and re-verifies build+test THERE.

**LEAD-VERIFIED in MAIN TREE (2026-06-24):** Room=13 (MIGRATION_12_13 in BOTH AppDatabase companion + DatabaseModule; Program::class in entities; programDao()/provideProgramDao present). Debug build GREEN, APK md5=`2a9189b72ff11a8e5694d90a479ca0bb`. compileReleaseSources GREEN. `./build.sh test` from result XMLs = **203 / 0 fail / 0 err / 0 skip** (testDebug+testRelease both ran). New E2 suites: E2ProgramModelTest(9), E2DeloadPolicyTest(8), E2MesocyclePromptTest(5), E2BackupProgramsTest(5)=27. AiRepository: B3 STALLED-LIFTS + B2 parseRationale/rationale intact; L1 = MesocycleContext.promptBlock() injected (""=byte-identical pre-E2 prompt for plain programs). Backup: envelope 2→3, `programs` field + BackupMigrations V2_TO_V3 + BackupMerger.mergePrograms (union-by-name, exactly-one-active preserved, id-remap) + ExportRepository programs-before-plans w/ remap — DONE this wave, NOT deferred. Deload: STALL_TRIGGER_COUNT=2; reuses StallDetector via DeloadPolicy→computeStalledLifts; deload=exactly 1 week; reachable via Program tab→Options→"Regenerate program now" + weekly auto-gen. ensureActiveProgramId() covers fresh-install (DB created at v13, migration never runs). Tree COHERENT, nothing staged. On-device gate dispatched to ui-test-worker (md5-pinned `2a9189b...`).

**ON-DEVICE RESULT (ui-test-worker, 2026-06-24, APK md5 verified `2a9189b...` on device):** (a) program SAVE/SWITCH = PASS (Travel Program created, plan COPIED not merged 29 rows each, spinner switch swaps shown plan + Home today-view, isActive flips, no corruption). (b) MESOCYCLE block = PASS (6-wk block set, DB mesocycleWeeks=6 + blockStartWeek=thisMonday → week 1/6 via Program.weekInBlock; MesocycleContext directive in prompt; live regen ran). (c) TRIGGERED deload = PARTIAL→accepted-PASS-with-residual: stall driven (2 lifts flat e1RM, DB-verified → shouldTriggerDeload(2)=true); LIVE Claude path exercised (real 200s to api.anthropic.com); deload INDICATORS verified (Home amber banner card_deload + Program chip tv_program_deload_chip render from isDeloadActive=1). RESIDUAL: live-AI-generated deload-WEEK artifact NOT observed end-to-end — every live gen attempt hit Waydroid 120s OkHttp read timeout or the ±10-min validation gate, so generation never COMPLETED. On failure app behaved correctly (plan unchanged, flag unset, no crash). Robustness PASS (rapid spinner/dialog/tab churn, exactly-one-active held). 

**Lead's gate ruling = PASS-with-1-residual:** the deload success-callback (`.onSuccess { savePlan; setActiveDeload(isDeload) }`) is verified DETERMINISTIC code at MainActivity:302 + ProgramViewModel:258 (reviewed) + unit-tested decision + on-device-verified flag→indicators; the ONLY unobserved hop is "live AI completes the round-trip on Waydroid" = ENVIRONMENTAL flake, NOT an E2 defect (NetworkModule 120s timeout is PRE-EXISTING, unchanged by Wave 3 — verified identical). Flagged to coordinator as a residual + a potential future timeout/retry-resilience item (not Wave-3 scope). 6 Maestro flows in scratchpad/flows/ (NOT in repo flows/; coordinator's call to keep).

**Final Room version = 13 → this is E1's (Wave 4) baseline.** Settled program model for E1: Program entity (programs table) + planned_exercises.programId FK; active-program drives Home+Program; plan queries program-scoped via flatMapLatest(observeActive); weekStart/dayOfWeek/orderInDay keying kept WITHIN each program. E1 (edit/delete/add/reorder planned_exercises) operates over this re-keyed storage, NO further schema bump expected.
