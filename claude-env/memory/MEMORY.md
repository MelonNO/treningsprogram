# Memory Index

- [UX-refine batch v1.25.1](project_ux_refine_batch_2026_07_03.md) — **SHIPPED v1.25.1 (2026-07-04, c00d9ac, verified live)**: 10 UI/UX polish items (Home action-first/collapsible weight card/hide-empty recovery/weigh-in undo, log chip-wrap + vector icons, Wrapped close, week-strip small-screen, dot-less eyebrows, empty-state voice). 1832 tests both variants. Shipped through severe API instability (~7 agent deaths incl. credit exhaustion); the **release was executed from the coordinator/main session** as a one-off exception because all subagents were credit-dead (user had authorized "Ship it now"). ~100MB APK upload must run backgrounded; a cut-off leaves a "starter" partial to DELETE before retry.
- [10-feature batch v1.25.0](project_feature_batch_2026_07_03.md) — **SHIPPED v1.25.0 (2026-07-03, ac9a7e7, coordinator-API-verified live, DB v19, backup v6)**: goal targets, relative strength (e1RM/BW), Home numbers-to-beat, effort→AI prompt, setup notes, warm-up ramp, rest-day card, monthly Wrapped, widget upgrade, level titles to Apex. 914 tests both variants; 0/15 live calls spent. NOT built (user chose): rep PRs, streak freeze, Wrapped sharing, goal-aware gen. On-device checks with user.

- [Staged run 2026-07-02→03](project_overnight_run_2026_07_02.md) — **✅ COMPLETE: FOUR releases shipped+verified in one run** (v1.22.0 rest-ux → v1.23.0 R1–R7 → v1.24.0 16-item UX → v1.24.1 patch); full Waydroid sweep done (one-night authorization SPENT — skip-Waydroid rule back in force); tests 685→840; 3 live API calls total. Report `docs/intake/overnight-run-2026-07-02/RUN-REPORT.md`. Open: user's on-device checklist (F3 proof, weight-pad dismiss, R7 flash, timer persistence, rest memory, notifications) + R3 gen-quality observation.
- [Stage-3 UX batch v1.24.0 + v1.24.1 patch](project_stage3_ux_batch_2026_07.md) — **SHIPPED v1.24.0 (fba7c08) + PATCH v1.24.1 (3b05561), both verified live**: all 16 UX items; 840 tests. v1.24.1 fixed the item-2 skeleton regression (History stuck — double stateIn chain never emitted; lesson: no two-layer shared flow chains) + BW×reps cosmetic. **Ratified: first-ever lifts = baselines, NOT PRs.** Keypad item-16 code correct (emulator tested the out-of-scope reps IME).
- [Feature-research batch v1.23.0](project_feature_research_batch_v1_23_0.md) — **SHIPPED v1.23.0 (2026-07-02, bd35ad9, coordinator-API-verified live, DB v18)**: R1–R7 gamification/insights batch (schedule-aware streak, notification center, body-weight insights, challenges 2.0, achievement gallery, celebration, beat-last-time); 803 tests both variants; crash-recovered mid-R7 (partial work had compile error, audited+finished). Release 2 of the staged run.
- [Week paging + Stats overhaul](project_week_paging_stats_overhaul_2026_07.md) — **SHIPPED v1.21.0 (2026-07-02, commits 33652bb+eb4da13, verified live, DB v18)**: Program week-card swipe-right → past logged weeks (read-only, HorizontalSwipeLayout), Stats visual overhaul (week pulse card w/ deltas, segmented rep-range capsule, gradient bars, chart glow, pill tabs), per-gym plate profiles (**home bar = 50 mm / 7 kg**, plates 20/15/10/5/2/1.45/1.25/1/0.5, loadable dumbbells; nullable GymPreset columns), Monday-week unification of all graphs. 685 tests. On-device checks pending.
- [Design-refine batch 2026-07](project_design_refine_2026_07.md) — **SHIPPED v1.20.0 (2026-07-02, commits 18a8323+97c5a13, tag+release API-verified live)**: coordinator coded DIRECTLY (user waived intake/orchestrator for this task). Full Auros deepening (R1–R7: 13 screens, aurora ghost borders, ↗ arrow links, particle-sphere/node art, dialogs/menus/snackbars, display numerals, palette strays) + 6 features (haptics, widget, reminders, plate readout, volume heatmap, what's-new) + audit: 3 UTC-vs-logical-day bugs fixed, **backup v4** (rest days/day boundary/auto-rebalance survive restore), **auto-gen retry-on-failure (cap 3)**. 677 tests green. On-device checks pending. Gotcha: `| tail` masks build exit codes.
- [Auros redesign 2026-07](project_auros_redesign_2026_07.md) — **SHIPPED v1.19.0 → polished in v1.19.1 (2026-07-01, commits 184c253 + d3403dd, both tag+release API-verified live, DB unchanged)**: complete dark-teal "Auros" visual redesign (spec `Change docs/DESIGN.md`). Theme/colors remap re-skins all 44 screens; bundled Inter variable font (weights via double-quoted `fontVariationSettings`); gamification accents kept vivid per user; eyebrow section headers on ~18 screens + hero bands on the 4 tabs; v1.19.1 added aurora hero wash, brighter/high-contrast day badges, Auros search icon, CTA arrow; **v1.19.2** (commit 9a88aa3) added the signature teal→cyan **gradient primary CTAs** (`GradientButton` extends MaterialButton + installs a gradient ripple itself, rationed 1/screen) + modal polish (rest-timer/onboarding/welcome eyebrows). Ship flow: signed release, stage app/-only, push+release via `~/.git-credentials` token. Install to device NOT done (no adb device; Waydroid stopped — user installs).
- [Flaky H5DispatcherTest](reference_flaky_h5dispatcher_test.md) — `consumesOnSuppliedDispatcherThread…` flakes in full test runs but passes in isolation; SSE dispatcher thread-timing, not a regression.
- [Settings UX batch (12 items)](project_settings_ux_batch_2026_07.md) — **SHIPPED v1.18.0 (2026-07-01, release commit cb20bf1, verified live, DB v17)**: 12-item UX batch — Settings IA restructure + new **App Settings** screen (day-reset + auto-rebalance), 24h labels, Debug→Backup, Coach→AI&Program, row reorder, Profile-tab-returns-to-root, auto-rebalance default ON, gen-loading on Program tab, "Generate now" preserves logged days, **Start-Workout merge + append-to-logged (data-safe)**, collapse rationale card, **per-muscle effort-scaled recovery** (replaces flat 48h). 651 tests (+19). Intake→orchestrator (branch, 4 commits)→coordinator shipped directly ("ship when done"). Residuals: on-device checks for items 3/8/10.
- [Long-session generation fix](project_long_session_fix_2026_07.md) — **SHIPPED v1.17.0 (2026-07-01, commit f170aed, verified live, DB v17)**: long (≥90 min) targets now REACH in-window via multi-modal structure (strength + duration-sized cardio finisher) — fixes the overnight-run "120-min produces nothing". Strict gate KEPT (no under-fill salvage; fail-loud). 626 tests, 20/20 live budget. **Follow-up SHIPPED v1.17.1 (commit b270d4b, verified live)**: `DURATION_AIM_BUFFER_MIN=12` aim-high calibration → **100-min now SAVES** (was nothing), strength@50 attempt-1 fixed, 120 no regression, 50 lean; 632 tests, 9/20 calls. Residual: short-rest goals @50 self-heal via ladder (goal-cardio steering out of scope). Coordinator shipped both v1.17.0 + v1.17.1 directly after the relayed-consent deadlock recurred.
- [Overnight autonomous chain](project_overnight_autonomous_chain_2026_07.md) — **✅ COMPLETE (2026-07-01)**. Full report: `docs/intake/overnight-run-2026-07/RUN-REPORT.md`. **S1 SHIPPED v1.14.0** (UX batch, 8 items). **S2 live gen-test, nothing shipped** (17/20 API calls; headline continuity+weights + 4 goals + injury + 20min PASS; **120-min produces nothing = top open NEEDS-ATTENTION**, needs product decision + live human iteration). **S3 SHIPPED v1.15.0** (dead-code cleanup, ~660 LOC). **S4 SHIPPED v1.16.0** (128 verbose exercise names now recognize 100%, DB v16→v17 backfill). All coordinator-verified live. Open for user: 120-min gen ceiling; optional colored icon-pack; on-device checks.
- [Generation quality overhaul](project_generation_quality_overhaul_2026_07.md) — **SHIPPED v1.13.0 (2026-07-01, commit 21b5682, tag+release API-verified live, DB unchanged v16)**: 6-brief prompt+validator overhaul from the two `Change docs/` feedback files. HEADLINE P1 fixes "exercises/weights off" (blacklist demoted, keep+progress anchors, ≤2 full swaps/wk, weights anchored to history). P2 deletes broken fixed-time constants → derives 20–120 min; P3 wide bands + 4-goal parity + no 120s ceiling; P4 empty-injury no-op; P5 §6 metadata; V1 de-false-rejects validateProgram (terminal ladder already existed). 568 tests. Behavioral ACs are live-gen-only (user checks on-device).
- [UX batch 2026-07](#) — **STALE ENTRY, RESOLVED**: the 8-item intake (briefs at `docs/intake/ux-batch-2026-07/`) was absorbed into the 12-item batch that **shipped as v1.18.0** (see [[project_settings_ux_batch_2026_07]]) — code carries "Item 1/7/8/9" markers (copy-all prompt-log, day boundary, last-session line, +/− keypad). Lucide icons present for nav/common icons; whether the FULL-app sweep (#6) is complete is unverified — treat #6 as the only possibly-open remnant.
- [Auto rest-day logging](project_auto_rest_day_logging.md) — **SHIPPED v1.12.0 (2026-06-28, commit dc8421e, DB v15→v16, release API-verified live)**: auto-logs every past empty day as REST (or MISSED if it was a scheduled training day) on app onStart; nullable `WorkoutSession.kind`, shown in History, excluded from all training stats. 567 tests pass. Shipped DIRECTLY by coordinator after the orchestrator re-hit the structural relayed-ship deadlock and the user gave a direct "ship directly" waiver. On-device unverified (user checks). Open (non-blocking): whether AI should see rest/missed context (currently excluded).

- [Program regen/rebalance batch](project_program_regen_rebalance_2026_06.md) — **SHIPPED v1.11.0 (2026-06-28, commit 13aefce, DB unchanged v15)**: 5 items — P4 single-day regen full weekly-parity (fixes all-bodyweight + same-workout bugs), P1 auto-rebalance-on-focus-change toggle, P2 "do another day's workout today" (commits on completion), P3 backgrounded gen-complete notification, P5 rotating wait tips. Intake-confirmed; one orchestrator pass; 556 unit tests. On-device + live-gen unverified (user checks on update).

- [Generation peer-review time-budget fix](project_generation_peer_review_timebudget_fix.md) — **SHIPPED v1.10.7 (2026-06-28, commit c6163ca)**: LLM peer-review (`validateProgram` item #13) falsely rejected in-window plans by pooling all sets into one rest chain → 3 failed attempts, nothing saved. One-line neuter of item #13 (deterministic gate already enforces duration BEFORE the review). Also logs the **ship-authorization deadlock**: orchestrator refused relayed "ship" consent (structurally unsatisfiable via agent boundary) → escalate to user, who waived "coordinator never ships" and had the coordinator ship directly.

- [Token/disk bloat cleanup](project_token_bloat_cleanup.md) — token-per-request driver was 7 stale agent worktrees (duplicate source trees, fan-out) + committed binaries; cleaned 2026-06-28 (.claude 4.6G→444K). Keep worktrees pruned; deeper lever is scoping the agent pipeline.
- [Treningsprogram project](project_treningsprogram.md) — Android adaptive workout planner (Kotlin/MVVM/Room/Hilt/Claude API), build env on Pi; now at **v1.8.0** (2026-06-24, DB v13): 8-feature batch (1RM/PR trends, recovery view, exercise library, plateau detection, program rationale, weekly coach summary, programs+mesocycles+deload, manual editing). Cloud backup still GATED on an unconfigured Google OAuth client ID; manual Export/Import is the live path.
- [Feature batch 2026-06](project_feature_batch_2026_06.md) — 8 features (B1/B2/B3/C1/C4/E1/E2/E3), 4-wave SEQUENCE. **DONE: all 4 waves coordinator-confirmed PASS → SHIPPED as v1.8.0** (commit a201ee0; github release live; APK md5 b545f6…). Non-blocking residuals/decisions for the user recorded in the file.
- [PR widget warm-up bug](project_pr_widget_warmup_bug.md) — known unfixed pre-existing bug: legacy Stats→Progress "Personal Records" widget counts warm-up sets as PRs (found during Wave 1 testing; NOT a regression; C1's new timeline is correct)
- [Achievements orphan rows](project_achievements_orphan_rows.md) — count shows >200 (e.g. 286) from un-pruned old achievement rows. UPDATE 2026-06-24: user REVERSED prior "don't apply" → now scheduled to FIX in the bug-sweep-ux-2026-06 batch (brief F1)
- [Bug-sweep + UX batch 2026-06](project_bug_sweep_ux_2026_06.md) — post-v1.8.0 whole-app bug sweep + UX batch at docs/intake/bug-sweep-ux-2026-06/ (Phase 1 bugs S1–S8 + F1/F2/F3, Phase 2 UX + 8 user-tested items; #5 B1-removal cancelled). STANDING: auto-dispatch orchestrator once understander is finished + crosschecked
- [No auto-release](feedback_no_auto_release.md) — never push a GitHub release unless explicitly asked
- [No unprompted testing](feedback_no_unprompted_testing.md) — do not run ADB/Waydroid app tests unless user explicitly asks
- [Release process](reference_release_process.md) — vMAJOR.MINOR.PATCH tags, treningsprogram-vX.Y.Z.apk naming, body template, curl-based API steps (no gh CLI)
- [No unrequested commits](feedback_no_unrequested_commits.md) — never stage or release working tree changes that weren't part of a user request
- [On-device test harness](reference_ondevice_test_harness.md) — Waydroid+adb+Maestro (NOT AVD; host is aarch64); bring-up steps, freeze gotcha, env vars, stale-APK/MD5 discipline, Maestro input quirks. **Reference only — do NOT run by default ([[feedback_always_skip_waydroid]]); use solely if the user explicitly revokes that.**
- [Always skip Waydroid](feedback_always_skip_waydroid.md) — **STANDING (2026-06-26): never run Waydroid/Maestro/on-device UI tests; `ui-test-worker` is dormant.** Verify via build + unit tests only; user does the device/live check. SUPERSEDES the old "prefer on-device verification" stance.
- [Intake agent: verbatim relay](feedback_intake_agent_verbatim_relay.md) — for the intake-understanding agent, always show its exact output and forward the user's replies straight to it
- [Orchestrator owns changes](feedback_orchestrator_owns_changes.md) — coordinator NEVER codes/edits/builds/tests/ships (UNCHANGED, absolute). **Pipeline SCALED TO RISK 2026-06-28:** small/low-risk single-unit → ONE orchestrator pass (no intake/ui-test); large/risky/ambiguous → full pipeline. **build-release-shipper REMOVED; orchestrator now builds AND ships.** Read-only tools are for VERIFYING agents' work, NOT primary diagnosis you then implement. (Coordinator-never-codes was reinforced after a 3rd violation 2026-06-26; only the always-full-pipeline part relaxed.)
- [Auth relay via coordinator](feedback_auth_relay.md) — actions needing user auth still need it; coordinator MAY relay auth both ways (agent asks user / user grants, incl. proactively) but must be NOTHING more than a messenger — never transcribe, assume, infer, or manufacture auth; applies to ALL agents (2026-06-24)
- [Intake batch 2026-06-25](project_intake_batch_2026_06_25.md) — 10-item refinements batch (post-v1.9.1). **SHIPPED as v1.10.0 (2026-06-26, commit 3e175b7 on main, DB unchanged): all 10 items (B01–B10) — rest-day two-mode scheduling, mid-week regen-preserves-logged-days, AI prose/truncation hardening + UX polish.** Recovered from a mid-run Pi crash (B01–B07/B10 were already done; orchestrator finished B08/B09). Release published + coordinator-verified live (asset `treningsprogram-v1.10.0.apk`). On-device verification was DEFERRED (user "skip waydroid, keep them"); B08/B09 Maestro flows authored + kept in `flows/` for a later run.
- [Exercise recognition fix](project_exercise_recognition_fix.md) — ~24 exercises mis-recognised by `MuscleClassifier`; pattern-level fix + tempo/interval false-cardio fix + one-time data-only backfill migration (DB 14→15, re-derives stored muscleGroup, never touches reps/weight). **SHIPPED v1.10.2 (2026-06-26, commit 2a7e938), release verified live.** Full pipeline. RESIDUAL: real-device migration-open path unverified (aarch64, no Robolectric; user confirms on first launch). Shipper raised a 3rd poisoning attempt — swept, nothing persisted.
- [Generation time-budget fix](project_generation_timebudget_fix.md) — post-v1.10.0 bug: AI made a valid plan but the strict per-day time-budget gate rejected every attempt → nothing saved (under-time days told to "trim"; prompt formula over-counted rest). Prompt-only fix (gate kept strict, no fallback). **SHIPPED v1.10.1 (2026-06-26, commit 38046a5 on main), release live + verified.** Done via the full pipeline (intake→orchestrator→shipper) after a process miss (see [[orchestrator-owns-changes]]). RESIDUAL: live generation unproven (user declined Waydroid + no API key) — user verifies on-device post-update; iterate to v1.10.2 if still off. Incidental unfixed: `MuscleClassifier` "tempo"/"interval" misclassifies strength names as cardio.
- [Generation retry-hang fix](project_generation_retry_hang_fix.md) — post-v1.10.2 bug: "Generate AI Program" froze on "Generating 2 of 3" + saved no plan. Root cause: time-gate rejected attempt 1 (days under 40 min) → attempt 2's oversized response hit the read-timeout → no-backoff retry burned ~8 min, error in an unseen Snackbar. Fixed H1 (no-retry on gen timeout + 360s deadline → terminal error), H3 (prompt steering: REST as #1 time lever + ordered under-fill; gate/caps untouched, no structural wall), H2 (blacklist built from structured names, fix comma corruption). Also surfaced: app has a **Prompt Log** (Profile tab); "verification prompt" = `validateProgram` peer-review call. SHIPPED v1.10.3 (commit d1d5625) — but it REGRESSED: large gen requests crossed the 180s read-timeout and v1.10.3's no-retry-on-timeout made it terminal+silent; H3 steering also never worked live. **Re-fixed + SHIPPED v1.10.4 (2026-06-27, commit c566063, DB unchanged, vc 42→43, release API-verified live)** via SSE streaming (kills the timeout) + revised "aim-higher/force ~19–20 sets@120s rest" steering + log-attempt-on-failure. User supplied a LIVE API key (`claude k`, gitignored) → first time generation was proven LIVE end-to-end (3/3 gate-pass). RESIDUAL: in-app Retrofit @Streaming wiring unexercised (Waydroid skipped) — user verifies on first generation after updating; session target >~55min would be a structural wall (user@50, fine). Orchestrator repeatedly stalled after watcher curls — coordinator nudged it via ground-truth checks. [[project_generation_timebudget_fix]]
- [Generation efficiency fix](project_generation_efficiency_fix.md) — **SHIPPED v1.10.6 (2026-06-27, commit 48fb61b)**: post-v1.10.5 "generation times out, saves nothing" → JSON-first efficiency prompt (kills the rambling, ~7-8× faster) + deterministic REST-first auto-trim salvage (saves an over-time-by-one-day plan instead of timing out). **Adaptive thinking on the gen call was live-tested and REJECTED — regressed hard (>508s/over deadline, full budget on thinking, no JSON, ~10× cost); do NOT re-try it.** Auto-trim unit-tested, not live end-to-end (spend limits). User reversed no-salvage stance once shown the strict gate is structurally unsatisfiable at 50-min/5-day.
- [Coordinator background-agent ops](feedback_coordinator_background_agent_ops.md) — driving long-running agents: ONE message then hold (don't churn — rapid messages eat their reports); "holding" isn't status — verify via TaskList + git/test ground truth (agents end without reporting); independently verify every "done" (shipper report was truncated, release object was missing); ship needs orchestrator's explicit all-clear + version (harness denies guessed version); after a security flag, verify no poisoned memory persisted (2026-06-25). **Desync seen v1.10.6 round: implementation workers reported their results to MAIN (coordinator), not to the orchestrator, so the orchestrator stalled waiting on "phantom" workers — coordinator unblocked it by feeding ground-truth (worktree grep/diff/test tallies) back to it.**
- [Frugal API testing](feedback_frugal_api_testing.md) — keep live Anthropic-API verification minimal + decision-driven (lean A/B, stop-early, ≤N loops, never re-verify proven items); a v1.10.6 sweep drained both the API key credits AND the monthly Claude Code spend limit
- [Agent usage guideline](feedback_agent_usage_guideline.md) — how to use the (now leaner) fleet: COORDINATOR spawns agents per task but still never codes/builds itself. **Orchestrator DEFAULTS to doing the work ITSELF (2026-06-28, was "primarily subagents")** — spawns workers only for genuine parallelism / isolation / independent verification; owns build + non-UI tests + version decision + **the release/ship step (build-release-shipper REMOVED 2026-06-28)**. intake runs first only for large/ambiguous work (risk-scaled, see [[orchestrator-owns-changes]]). ui-test-worker = Waydroid/Maestro UI verification but **DORMANT/SKIPPED by standing instruction ([[feedback_always_skip_waydroid]])** — verify via unit tests only.
