---
name: project_settings_ux_batch_2026_07
description: "v1.18.0 SHIPPED — 12-item Settings/UX batch: Settings IA restructure + new App Settings screen, generation-from-Settings fixes, Start-Workout merge w/ append-to-logged, per-muscle effort-scaled recovery."
metadata: 
  node_type: memory
  type: project
  originSessionId: db92b508-221f-4bb4-bead-47a455671941
---

**SHIPPED v1.18.0 (2026-07-01, release commit `cb20bf1` on main, MINOR, DB unchanged v17, versionCode 55, tag+release API-verified live).** 12-item user UX/behavior batch (user's own numbering 1–12). Intake by the intake-understanding agent (briefs in `docs/intake/settings-ux-batch-2026-07/`), built by a fresh project-lead-orchestrator on branch `settings-ux-batch-2026-07` (4 feature commits `0ec0b2e`/`70727bd`/`b6f0bf1`/`a12e4e8`, ff-merged to main + a release commit). **651 unit tests (+19 new: Item10=8, Item12=9, Item9=2, U1Weighted expanded). Coordinator-verified: build clean, 28 files all in `app/`, item-10 data-safety read + confirmed non-destructive.** User pre-authorized ("kick it off now ship when done"); coordinator shipped directly.

Items: **1** 24-hour day-reset labels (no AM/PM); **2** auto-rebalance default ON for anyone who never explicitly set it (getter default=true, key written only on explicit toggle — no sentinel); **3** Profile bottom-nav returns to Profile root from any settings sub-screen in one tap (handled both select+reselect); **4** new **App Settings** screen holding day-reset + auto-rebalance (moved out of Training Profile / Program-options dialog); **5** Debug → under Backup & Data; **6** Coach Summary → under AI & Program; **7** row reorder (Training Profile, AI & Program, Exercise Library, Backup & Data, App Settings, About); **8** generation loading animation on Program tab (new `GenerationState.kt`, no auto-switch, additive to Settings status); **9** Settings "Generate now" preserves logged days + rebalances (routes through `doGenerate`→`savePlanPreservingLoggedDays`, same as ProgramVM.runRebalance); **10** removed "Do this workout today" → Start Workout on another day moves it to today; if today already logged, APPENDS into that session (one continuous History entry). Data-safe: `startAppendableTodaySession` only flips `isCompleted=false` (never touches logged sets); abandon on a reopened append calls `recompleteSession` (restore) NOT delete (`isReopenedAppend` flag). **11** "Why your program changed" collapsed by default; **12** per-muscle recovery — deterministic on-device base-recovery table (fine-grain; Chest kept 48h for C4/U1 tests, larger 60–72h, small 36h, cardio 24h) × effort multiplier (MAX RPE per exercise/session via new `effortLevel` DAO projection); blank RPE → medium (1.0).

RESIDUALS (on-device unverified — Waydroid dormant; user checks): item 3 one-tap-to-root, item 8 loading animation, item 10 reopen/append/abandon-restore end-to-end. Also: starting an already-logged OTHER day still offers Start Workout (pre-existing v1.14.0 behavior, not enforced-blocked); abandoning a reopened append KEEPS that segment's sets (chose never-lose-data). Flaky `H5DispatcherTest` (thread-affinity, unrelated) passed on rerun.
